import { ReplaceUnderscoreSpacePipe } from './replace-underscore-space.pipe';

describe('ReplaceUnderscoreScapePipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceUnderscoreSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
